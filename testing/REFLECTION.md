

 I started this project on the 1st of August however i spent the day before brainstorming what i could do and eventually settled on doing a colour guessing game. I wanted to be able to record wether the player won or lost and have that information be put into a csv file to later be printed out. However, when working on it I was unable to figure out how to do that and instead opted for an arcade style print out listing the scores and names of previous players. So far i had used the print command learned in the first session of my programming classes to print an introduction for the user. As well as this i started writing code to append to a csv file which i planned to develop later on. And I made a function that would pick a random colour for the user in my 'Testingimport.py' file that was intended to be imported into my 'Assignment.py' file.
 
 From the 2nd-4th of August i removed the introduction and replaced it with a menu and the opportunity for the user to input their name. Imported the random colour function. At this stage it is a custom libary however that will be changed soon. As well as this i created two questions for the user to answer. One of them is imported from a custom libary which is going to be developed with more questions to import into the main file.

 On August 5th-6th i changed the code from '==' to ':=' so that it would also run the else function. I began attempting to create a custom libary using regex and set up code that will either accept or decline the username the user has inputed. Aswell as this i built upon the code already there by adding new questions for the user to answer so the computer can narrow down what the colour is.

 On August 10th i fully completed the regex code and chose to make the defenition in the main code instead of importing it as a libary.

 On August 11th i cleaned up my code and because i was experiencing problems with my else and if function i retyped it into a new file slowly in order to figure out what was going on and how to fix it. 

 On August 12th the guessing game code was fully finish and i began working on other aspects of my code.

 August 15th is when i began experimenting with the csv file. I was aiming to make it record the users losses and wins however i could not do that so i made it print out information in order to imitate an arcade score board.

 August 17th -18th I made code comments and attempted to do testing of my code. 

 Upon reflection i would spend more time planning out what i wanted to do and researching before starting my project. As i felt that looking at File I/O and OOP when i had finished the main bulk of code made it more difficult to implement. It seemed like it didn't fit in as well with the rest of the code. In the future i will research all aspects before diving right in and make an indepth detailed plan in order to help me implement everything into my code. As well as this i will do my testing during my coding because it would be more beneficial in order to help me understand any mistakes and where i could improve. I think that i also could have expanded the code i used for example instead of just using else and if functions i could have used the OOP to create less basic more complicated code in order for it to run more seemlessly and seem more like an actual game. This would help me expand my coding knowledge aswell. If i had more time i would have spent it on trying to figure out how to append to a csv file. However i learnt important information about csv files in my research. Such as 'w' means write. 'r' means read. And 'a' means append. This information was useful and i think alot of the things i researched when doing this project helped me to expand my knowledge and will be important for future projects.
